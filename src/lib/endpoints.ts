const raceTypes = {
  base: "/api/race-type",
};

const eventTypes = {
  base: "/api/event-type",
};

const feeSchemes = {
  base: "/api/fee-scheme",
};

const prizeSchemes = {
  base: "/api/prize-scheme",
};

const bettingSchemes = {
  base: "/api/betting-scheme",
};

export const apiEndpoints = {
  raceTypes,
  eventTypes,
  feeSchemes,
  prizeSchemes,
  bettingSchemes
};
