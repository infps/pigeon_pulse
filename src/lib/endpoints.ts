const raceTypes = {
  base: "/api/race-type",
};

const eventTypes = {
  base: "/api/event-type",
};

const feeSchemes = {
  base: "/api/fee-scheme",
};

const prizeSchemes = {
  base: "/api/prize-scheme",
};

const bettingSchemes = {
  base: "/api/betting-scheme",
};

const users = {
  base: "/api/users",
};

const events = {
  base: "/api/event",
};

export const apiEndpoints = {
  raceTypes,
  eventTypes,
  feeSchemes,
  prizeSchemes,
  bettingSchemes,
  users,
  events,
};
